#include "main.h"

/**
 * get_endianness - This checks the endianness (big or little).
 * Return: 0 if big, 1 if little endian.
 */
int get_endianness(void)
{
unsigned int g = 1;
char *b = (char *) &i;

return (*b);
}
